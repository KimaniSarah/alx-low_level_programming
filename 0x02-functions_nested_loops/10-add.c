#include "main.h"
/**
 * add - adds two integers and returns the result.
 * @x: first num
 * @y: second num
 *
 * Return: result
 */
int add(int x, int y)
{
	int results;

	results = x + y;

	return (results);
}
